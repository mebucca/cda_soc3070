---
title: "Análisis de Datos Categóricos (SOC3070)"
subtitle: "Estimación, MLE y Funciones de Pérdida"
author: "Mauricio Bucca · Sociología UC"
date: today
body-classes: theme-tottenham
course-team: tottenham
filters: ["../_shared/course.lua"]
format:
  revealjs:
    css: "../_shared/course.css"
    slide-number: false
    theme: default
    highlight-style: github
    code-block-font-size: 0.9em
    width: 1600
    height: 900
    html-math-method: katex
---

```{r course-style, include=FALSE}
library(pacman)
source("../_shared/bikeshare.R")
course_palette <- use_course_style("tottenham")
```

## Antes · Hoy · Después {.class-rhythm}

::: {.full-width}

::: {.content-box}
**Antes**

valor esperado y varianza.
:::

::: {.content-box-primary}
**Hoy**

máxima verosimilitud (MLE) y funciones de pérdida.
:::

::: {.content-box-secondary}
**Después**

regresión lineal y el modelo lineal de probabilidad.
:::

:::

# Estimación {.inverse .center .middle}
## Maximum Likelihood Estimation (MLE)

## Estimación

::: {.center}

![estimado](estimado.jpeg)

:::

## Estimación
[Modelos estadísticos]{.bold}:  ¿Cuáles son los valores más [plausibles]{.bold}[1]

::: {.footnote}

[1] Notar que no dice "más probables"!

:::

 de los *parámetros* dado los *datos* que observamos?
Ej. Supongamos que alguien lanza 100 veces la misma moneda y registra los resultados en una base de datos. Los datos se ven así:

::: {.pull-left}

```{r, echo=FALSE, fig.height=5, fig.width=6,  message=FALSE, warning=FALSE}
p_load("tidyverse")
set.seed(481)

data_coins <- data.frame(X = rbinom(n=100, size=1, prob=0.8))

data_coins %>%
  ggplot(aes(x=factor(X))) +
    geom_bar(aes(fill=factor(X)), width = 1, color = "black") +
    geom_text(aes(label=..count.., y = ..count..), stat='count', vjust=-0.5, size = 5) +
    scale_fill_manual(values = c(course_palette[["accent"]], course_palette[["accent"]])) +
    labs(y="Recuentos", x="", title="") +
    guides(fill=FALSE) +
    ylim(0,90) +
    theme_julia() +
    theme(
      axis.text.y = element_text(size = 22),
      axis.text.x = element_text(size = 22),
      axis.title.y = element_text(size = 24),
      axis.title.x = element_text(size = 24),
      panel.border = element_blank()
    )
```

:::

. . .

::: {.pull-right}

- Lo que vemos en la izquierda son [datos]{.bold}
- Datos: realización de $n$ variables aleatorias
- Normalmente *no conocemos* la distribución de las variables
- Datos nos dan una pista sobre cuál podría ser esa distribución
- [Estadística]{.bold}: aprender de los datos para [*estimar*]{.bold} los parámetros que los generan

:::

## Estimación via Maximum Likelihood (MLE)
Previamente lanzamos la misma moneda 100 veces y obtuvimos "Cara" (1) 82 veces.

. . .

 ¿Qué valor de $p$ es más plausible ("likely") que genere estos datos?
MLE es justamente la formalización de esta pregunta. Pasos:

. . .

1) Decidir sobre la distribución subyacente que genera los datos. En este caso, podemos asumir que:
  * Cada lanzamiento $X_{1}, X_{2}, \dots X_{100} \sim \text{Bernoulli}(p)$, donde X's son $iid$

. . .

2)  Escribir una función que cuantifique la plausibilidad de diferentes valores del parámetro. Dicha función se denomina [likelihood function]{.bold}:

  * $\mathcal{L}(p \mid \text{ Datos}) = \mathbb{P}(\text{ Datos : \{1,0,1,1,....0,1\}} | \text{ } p)$

. . .

  * $\mathcal{L}(p \mid \text{ Datos}) = \mathbb{P}(x_{1})\mathbb{P}(x_{2}) \dots \mathbb{P}(x_{100}) = p^{82}(1-p)^{18}$

## Estimación via Maximum Likelihood (MLE)
Podemos inspeccionar visualmente la "likelihood" de diferentes valores $p$.

::: {.center}

```{r, echo=FALSE, fig.height=5, fig.width=9,  message=FALSE, warning=FALSE}
p_load("tidyverse")

plot <- ggplot(data = data.frame(p = 0), mapping = aes(x = p))

binom_distrib <- function(p,n,k) (p^(k))*((1-p)^(n-k))

plot +
  stat_function(fun = binom_distrib, args = list(n= 100, k= 82), size=1.5, color = course_palette[["accent"]]) +
  xlim(0,1) +
  labs(title="Likelihood of p", x="p", y=expression(paste(p^{82}, (1-p)^{18}))) +
  guides(fill=FALSE, color=FALSE) +
  theme_julia() +
  theme(
    axis.text.y = element_text(size = 22),
    axis.text.x = element_text(size = 22),
    axis.title.y = element_text(size = 24),
    axis.title.x = element_text(size = 24),
    panel.border = element_blank()
  )

```

:::

Intuitivamente: habiendo obtenido 82 caras, $p=0.82$ es el valor más plausible de $p$

## Estimación via Maximum Likelihood (MLE)

3) Encontrar matemáticamente el valor de $p$ que maximiza $\mathcal{L}(p \mid \text{ Datos})$.
- $\mathcal{L}(p \mid \text{ Datos}) = \mathbb{P}(x_{1})\mathbb{P}(x_{2}) \dots \mathbb{P}(x_{n}) =\prod_{i=1}^{n} f(x_{i}) =  p^{k}(1-p)^{n-k} \quad \text{   donde  } k= \sum x_{i}$

. . .

- Para facilitar el cálculo tomamos logaritmo natural en ambos lados ([log-likelihood]{.bold})
  - $\ell\ell(p) = \ln \mathcal{L}(p \mid \text{ Datos})  = k \ln(p) + (n - k) \ln(1-p)$

. . .

-  Calcular la primera* derivada de $\ell\ell(p)$ con respecto a $p$: pendiente de la curva en cada valor de $p$.
  - $\ell\ell^{\text{ '}}(p) = \frac{k}{p} -  \frac{n-k}{1-p}$

. . .

- Encontrar el máximo de la función $\ell\ell(p)$: valor de $p$ en el cual la curva no cambia, es decir cuando $\ell\ell^{\text{ '}}(p)=0$
  - $\frac{k}{p} -  \frac{n-k}{1-p} = 0$

. . .

- Después de resolver por $p$ obtenemos:
   $$p = \frac{k}{n} = \frac{\sum x_{i}}{n}$$

## Estimación via Maximum Likelihood (MLE)

- El estimador ML de $p$ es ....
- $\hat{p} = \frac{\sum x_{i}}{n}$
- Es decir, el porcentaje de 1's en la muestra!

. . .

- Intuitivo y elegante

## Estimación via Maximum Likelihood (MLE)
[Generalización]{.bold}

$$\hat{\boldsymbol{\beta}}_{MLE} = \underset{\beta}{\arg\max\ } \mathcal{L}(\boldsymbol{\beta} \mid \boldsymbol{X})$$
$\hat{\boldsymbol{\beta}}$ es el MLE de $\boldsymbol{\beta}$ si es el(los) valor(es) que maximiza(n) la "likelihood function", condicional en los datos observados.

. . .

- Recordar que   $\mathcal{L}(\boldsymbol{\beta} \mid \boldsymbol{X}) = \mathbb{P}(\boldsymbol{X} \mid \boldsymbol{\beta})$.

. . .

- Requiere especificar de antemano la distribución conjunta de las observaciones (dif. de OLS, por ejemplo).

. . .

- ML es probablemente el approach de estimación más popular.

. . .

- Intuitivo, pero, por lo general, no tan simple como el ejemplo que vimos hoy.

. . .

- Normalmente la maximización se realiza numéricamente (ej. método Newton–Raphson)

## Estimación via Maximum Likelihood (MLE) {background-image="valdorcia.jpeg" .fullscreen .left .top .text-white}

# Funciones de pérdida {.inverse .center .middle}

## De maximizar a minimizar

Todo lo que hicimos arriba tiene una lectura equivalente, muy usada en machine learning: en vez de **maximizar la verosimilitud**, podemos **minimizar una función de pérdida (loss function)**.

. . .

- Maximizar $\mathcal{L}(p\mid\text{Datos})$ ⟺ Maximizar $\ell\ell(p) = \ln\mathcal{L}(p\mid\text{Datos})$ ⟺ Minimizar $-\ell\ell(p)$.
- El signo negativo es la única diferencia: "ajustar bien" y "perder poco" son la misma cosa vista al revés.

## La pérdida, para nuestro mismo ejemplo

Con $n$ lanzamientos y resultados $y_1,\dots,y_n\in\{0,1\}$, la log-verosimilitud de Bernoulli que ya escribimos es:

$$
\ell(\boldsymbol{p}) = \sum_{i=1}^{n} \big[ y_i \ln p_i + (1-y_i) \ln(1-p_i) \big]
$$

. . .

En ML a $-\ell(\boldsymbol p)$ le llamamos **log-loss** o **cross-entropy loss**:

$$
\text{LogLoss} = -\ell(\boldsymbol p)
$$

- Si las predicciones son correctas ($y=1$ y $p$ cercano a 1) → log-loss bajo.
- Si se equivocan ($y=1$ pero $p$ cercano a 0) → log-loss alto.
- Entrenar = **ajustar los parámetros para minimizar log-loss** (= maximizar likelihood).

## Log-loss promedio

En machine learning se suele trabajar con la **log-loss promedio**, para obtener una pérdida por observación (comparable entre datasets de distinto tamaño):

$$
\text{LogLoss} = -\frac{1}{n}\,\ell(\boldsymbol p)
$$

```{r}
# usando nuestro ejemplo de 100 lanzamientos, 82 caras
p_hat <- 0.82
y <- c(rep(1, 82), rep(0, 18))
p <- rep(p_hat, 100)
log_loss <- -mean(y * log(p) + (1 - y) * log(1 - p))
log_loss
```

Es exactamente $-\ell\ell(\hat p)/n$: la misma cantidad que maximizamos arriba, reescalada y con el signo invertido.

## Otras funciones de pérdida

El mismo principio —MLE bajo un supuesto de distribución equivale a minimizar una pérdida específica— se generaliza más allá de Bernoulli:

| Outcome | Distribución supuesta | Pérdida asociada |
|---|---|---|
| Binario (0/1) | Bernoulli | log-loss / cross-entropy |
| Continuo | Normal | error cuadrático medio (MSE) |
| Conteo | Poisson | devianza de Poisson |

No son fórmulas sueltas: **toda pérdida que usaremos en este curso es, en el fondo, una log-verosimilitud negativa** de algún supuesto sobre cómo se generan los datos.

## Por qué esto importa para lo que viene

- Cuando ajustemos un GLM (clase 5) o una regresión logística (clase 6), **estimar el modelo** va a significar, otra vez, encontrar los parámetros que maximizan una verosimilitud — o minimizan la pérdida asociada.
- Cuando tratemos la regresión logística como **clasificador** (clase 15), log-loss va a ser exactamente la métrica que usemos para comparar modelos.
- Cuando lleguemos a **regularización** (clase 16) y **redes neuronales** (clase 18), vamos a agregar un término extra a esta misma pérdida para controlar la complejidad del modelo — pero la pérdida de base sigue siendo esta.

La idea de hoy —likelihood y pérdida son dos caras de la misma moneda— es una de las piezas que más se repite en el resto del curso.

## En resumen

- MLE encuentra los parámetros que hacen más plausibles los datos observados.
- La condición de primer orden $\ell\ell'(\hat\theta)=0$ es, en general, cómo se resuelve ese problema.
- Minimizar una función de pérdida es lo mismo que maximizar una verosimilitud, con el signo invertido.
- Log-loss (cross-entropy) es la pérdida asociada a un outcome Bernoulli — la usaremos una y otra vez en el resto del curso.

## Gracias {.inverse .center .middle .closing}

**Hasta la próxima clase**

Mauricio Bucca
